/* autoexec: cap input rows for the captured run. This bundle is
   fully self-contained (the script simulates its own ADSL-like data
   with CALL STREAMINIT(12345) for a fixed seed), so no mock input
   datasets are needed here. */
options obs=100;
